# COMIT Deployment Sentinel

This file intentionally changes when the Vercel Git connection is repaired.

The deployment pipeline must verify the `comit` root directory, run the regression guard, build successfully, and deploy the latest `main` commit to production.
